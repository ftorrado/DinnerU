<% if comment.user == current_user && !parent.nil? %>
  <span class="pull-right">
    <% # TODO: find beter way to do this
      if comment.meal.nil?
        comment_path = meal_order_comment_path(parent.meal, parent,
                                               comment)
      else
        comment_path = meal_comment_path(parent, comment)
      end
    %>
    <%= link_to 'Remove', comment_path, method: :delete,
                data: { confirm: 'Are you sure?' },
                class: 'btn btn-sm btn-danger' %>
  </span>
<% end %>

<h4 class="list-group-item-heading">
  <%= comment.user.name %>
</h4>
<p class="list-group-item-text">
  <%= comment.text %>
</p>