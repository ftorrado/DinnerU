<% if parent.comments.exists? %>
  <ul class="list-group">
    <% parent.comments.each do |comment| %>
      <li class="list-group-item clearfix">
        <%= render comment, parent: parent %>
      </li>
    <% end %>
  </ul>
<% end %>